apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: conference
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
# Service for intern discovery